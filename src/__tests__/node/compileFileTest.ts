import main ,{createCSSFileByFile,postify, watch} from '../../node/compileFile'
import postcss from 'postcss';
import postcssDiscardDuplicates from 'postcss-discard-duplicates';
describe('Create CSS File By File',()=>{
test("Test css create file",()=>{
    true;
    //expect(watch({input:'./demo/**/*.html',output:{location:'./demo/acss.css','--file':true}})).toBe('');
     
})
})